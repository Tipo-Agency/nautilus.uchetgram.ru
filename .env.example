# Backend
DB_PASSWORD=taska
SECRET_KEY=change-me-in-production

# Telegram Bot (для docker-compose)
# TELEGRAM_BOT_TOKEN=your-bot-token

# Frontend (Vite): base URL for API. Default /api/v1. For production use same-origin /api/v1.
# VITE_API_URL=/api/v1
